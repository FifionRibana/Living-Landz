[package]
name = "client"
version.workspace = true
edition.workspace = true

[[bin]]
name = "client"
path = "src/main.rs"

[dependencies]
shared = { path = "../shared" }
hex_grid = { path = "../hex_grid" }

bevy = { workspace = true }
log = { workspace = true }
serde = { workspace = true }
bincode = { workspace = true }
tungstenite = { workspace = true }

[features]
default = ["dev"]
dev = ["bevy/dynamic_linking"]  # Hot reload en dev